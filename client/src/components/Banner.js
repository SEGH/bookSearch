import React from "react";

export default function Banner() {
    return (
        <section id="banner" className="center z-depth-2">
            <h2 className="hide-on-small-only">(React) Google Books Search</h2>
            <h5 className="flow-text">Search for and Save Books of Interest</h5>
        </section>
    );
}
